# 备选方案：容器内 nginx 一体化编排（app + nginx 两个服务）
#
# 适用：不想维护宿主机 nginx，希望 Docker 一并管理反代与 HTTPS。
# 切换方式：换用本套编排即可，应用镜像与构建流程完全不变，两方案可随时切换。
#
# 用法（服务器上）：
#   cp docker-compose.nginx.yml.example docker-compose.yml
#   cp nginx-container.conf nginx.conf
#   docker compose up -d
#
# 证书：宿主机 certbot 签发（/etc/letsencrypt），挂载给 nginx 容器只读。

services:
  app:
    image: ghcr.io/stationpc/stationnas-docs:latest
    restart: unless-stopped
    expose:
      - "3000"
    networks:
      - web

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - app
    networks:
      - web

networks:
  web:
    driver: bridge
